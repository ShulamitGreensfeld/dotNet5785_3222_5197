<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Main Management Screen" Height="800" Width="1000"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Closed="MainWindow_Closed"
        Background="#EAF6F6"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen">

    <!-- Global Styles and Resources -->
    <Window.Resources>
        <!-- General style for modern turquoise square buttons -->
        <Style x:Key="ModernButton_Square" TargetType="Button">
            <Setter Property="Background" Value="#1ABC9C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="6" ShadowDepth="2" Color="#888"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                BorderThickness="0"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border"
                                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                            To="#16D2B2" Duration="0:0:0.25"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border"
                                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                            To="#1ABC9C" Duration="0:0:0.25"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#139C89"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Clock Header Bar -->
        <DockPanel Grid.Row="0" LastChildFill="True" Margin="0 0 0 20">
            <Label Content="{Binding CurrentTime, StringFormat=dd/MM/yyyy}" 
                   FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                <Button Style="{StaticResource ModernButton_Square}" Content="⏩ Minute" Click="btnAdvanceMinute_Click" Margin="5 0" Height="30"/>
                <Button Style="{StaticResource ModernButton_Square}" Content="⏩ Hour" Click="btnAdvanceHour_Click" Margin="5 0" Height="30"/>
                <Button Style="{StaticResource ModernButton_Square}" Content="⏩ Day" Click="btnAdvanceDay_Click" Margin="5 0" Height="30"/>
                <Button Style="{StaticResource ModernButton_Square}" Content="⏩ Month" Click="btnAdvanceMonth_Click" Margin="5 0" Height="30"/>
                <Button Style="{StaticResource ModernButton_Square}" Content="⏩ Year" Click="btnAdvanceYear_Click" Margin="5 0" Height="30"/>
            </StackPanel>
        </DockPanel>

        <!-- Risk Range Update Section -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 20" VerticalAlignment="Center">
            <TextBox Width="200" Text="{Binding RiskRange, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 10 0"/>
            <Button Style="{StaticResource ModernButton_Square}" Content="⚙ Update Risk Range" Width="200" Click="btnUpdateRiskRange_Click"/>
        </StackPanel>

        <!-- Main Operations Area -->
        <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center" Width="320">
            <Button Style="{StaticResource ModernButton_Square}" Content="📋 Open List View" Height="50" Margin="0 5" Width="250"/>
            <Button Style="{StaticResource ModernButton_Square}" Content="🛠 Initialize Database" Height="50" Margin="0 5" Width="250"/>
            <Button Style="{StaticResource ModernButton_Square}" Content="🔄 Reset Database" Height="50" Margin="0 5" Width="250"/>
            <Button Style="{StaticResource ModernButton_Square}" Content="🤝 Handle Volunteers" Height="50" Margin="0 5" Width="250" Click="btnVolunteers_Click"/>
        </StackPanel>
    </Grid>
</Window>