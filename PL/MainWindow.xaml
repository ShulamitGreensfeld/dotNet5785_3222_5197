<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="Main Management Screen" Height="800" Width="1000"
        Background="#0D1117"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI">

    <Window.Resources>
        <local:BoolToStartStopTextConverter x:Key="BoolToStartStopTextConverter"/>
        <local:InverseBoolConverter x:Key="InverseBoolConverter"/>

        <Style x:Key="ModernButton_Square" TargetType="Button">
            <Setter Property="Background" Value="#238636"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderBrush" Value="#238636"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#6EE7B7"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#161B22"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Name="HourglassOverlay"
 Background="#80000000"
 Visibility="Collapsed"
 HorizontalAlignment="Stretch"
 VerticalAlignment="Stretch"
 Panel.ZIndex="9999">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="⏳" FontSize="90" HorizontalAlignment="Center"/>
                <TextBlock Text="Initializing..." FontSize="30" Foreground="White" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
        <!-- clock -->
        <Label Content="{Binding CurrentTime, StringFormat=HH:mm:ss dd/MM/yyyy}"
               FontSize="22"
               FontWeight="Bold"
               Foreground="White"
               HorizontalAlignment="Center"
               Margin="0,10,0,20"/>

        <!-- simulator -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,20" Grid.Row="0">
            <TextBlock Text="Simulator Interval (minutes):" FontWeight="Bold" FontSize="15"/>
            <TextBox Width="80" Text="{Binding Interval, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     IsEnabled="{Binding IsSimulatorRunning, Converter={StaticResource InverseBoolConverter}}"/>
            <Button Style="{StaticResource ModernButton_Square}"
                    Content="{Binding IsSimulatorRunning, Converter={StaticResource BoolToStartStopTextConverter}}"
                    Command="{Binding StartStopSimulatorCommand}"/>
        </StackPanel>

  
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,20">
            <Button Style="{StaticResource ModernButton_Square}" Content="⏩ Minute"
                    Command="{Binding PromoteMinuteCommand}"
                    IsEnabled="{Binding IsSimulatorRunning, Converter={StaticResource InverseBoolConverter}}" Margin="5"/>
            <Button Style="{StaticResource ModernButton_Square}" Content="⏩ Hour"
                    Command="{Binding PromoteHourCommand}"
                    IsEnabled="{Binding IsSimulatorRunning, Converter={StaticResource InverseBoolConverter}}" Margin="5"/>
            <Button Style="{StaticResource ModernButton_Square}" Content="⏩ Day"
                    Command="{Binding PromoteDayCommand}"
                    IsEnabled="{Binding IsSimulatorRunning, Converter={StaticResource InverseBoolConverter}}" Margin="5"/>
            <Button Style="{StaticResource ModernButton_Square}" Content="⏩ Month"
                    Command="{Binding PromoteMonthCommand}"
                    IsEnabled="{Binding IsSimulatorRunning, Converter={StaticResource InverseBoolConverter}}" Margin="5"/>
            <Button Style="{StaticResource ModernButton_Square}" Content="⏩ Year"
                    Command="{Binding PromoteYearCommand}"
                    IsEnabled="{Binding IsSimulatorRunning, Converter={StaticResource InverseBoolConverter}}" Margin="5"/>
        </StackPanel>

        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0"
                        VerticalAlignment="Top"
                        HorizontalAlignment="Center"
                        Width="320"
                        Margin="0,0,20,0"
                        Orientation="Vertical">

                <Button Style="{StaticResource ModernButton_Square}" Content="🛠 Initialize Database" Width="250" Height="45" Margin="0 5"
                        Command="{Binding InitializeDatabaseCommand}"/>
                <Button Style="{StaticResource ModernButton_Square}" Content="🔄 Reset Database" Width="250" Height="45" Margin="0 5"
                        Command="{Binding ResetDatabaseCommand}"/>
                <Button Style="{StaticResource ModernButton_Square}" Content="🤝 Handle Volunteers" Width="250" Height="45" Margin="0 5"
                        Command="{Binding OpenVolunteersCommand}"/>
                <Button Style="{StaticResource ModernButton_Square}" Content="📞 Handle Calls" Width="250" Height="45" Margin="0 5"
                        Command="{Binding OpenCallsCommand}"/>

                <StackPanel Orientation="Horizontal" Margin="0,15,0,5" HorizontalAlignment="Center">
                    <TextBox Width="100" Text="{Binding RiskRange, UpdateSourceTrigger=PropertyChanged}"/>
                    <Button Style="{StaticResource ModernButton_Square}" Content="⚙ Update Risk Range" Width="170"
                            Command="{Binding UpdateRiskRangeCommand}" Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5,0,0" HorizontalAlignment="Center">
                    <TextBox Width="100" Text="{Binding SimulatorSpeed, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                    <Button Style="{StaticResource ModernButton_Square}" Content="⚙ Set Speed" Width="110"
                            Command="{Binding SetSpeedCommand}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" VerticalAlignment="Top" HorizontalAlignment="Center" Width="400" Margin="40 0">
                <TextBlock Text="Call Quantities by Status"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="White"
                           Margin="0 0 0 10"
                           TextAlignment="Center"/>

                <ListView ItemsSource="{Binding CallQuantities}"
                          Background="#161B22"
                          BorderBrush="#30363D"
                          Foreground="White"
                          SelectionMode="Single"
                          Margin="0 0 0 20">

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Background" Value="#161B22"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border x:Name="Border"
                                                Background="{TemplateBinding Background}"
                                                CornerRadius="4">
                                            <ContentPresenter Margin="0"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#21262D"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="5">
                                <TextBlock Text="{Binding Status}" FontWeight="Bold" Width="200"/>
                                <TextBlock Text="{Binding Quantity}" Width="50" TextAlignment="Right"/>
                                <Button Content="View"
                                        Command="{Binding DataContext.ViewCallsCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding Status}"
                                        Style="{StaticResource ModernButton_Square}"
                                        Width="80" Margin="10 0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>

    </Grid>
</Window>
