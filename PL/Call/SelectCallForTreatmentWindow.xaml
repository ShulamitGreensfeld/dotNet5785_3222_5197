<Window x:Class="PL.SelectCallForTreatmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Select Call for Treatment" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Filters -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
            <RowDefinition Height="Auto"/>
            <!-- Update Address -->
        </Grid.RowDefinitions>

        <!-- Filter and Sort Options -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10" VerticalAlignment="Top">
            <TextBlock Text="סנן לפי סוג קריאה:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding CallTypeCollection}" SelectedItem="{Binding SelectedCallType ,Mode=TwoWay}" Width="120" Margin="0,0,20,0"/>
            <TextBlock Text="סנן לפי כתובת:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Text="{Binding AddressFilter, UpdateSourceTrigger=PropertyChanged}" Width="120" Margin="0,0,20,0"/>
            <TextBlock Text="מיין לפי:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding SortOptions}" SelectedItem="{Binding SelectedSortOption}" Width="120"/>
        </StackPanel>

        <!-- Calls List -->
        <DataGrid Grid.Row="1" AutoGenerateColumns="False" ItemsSource="{Binding OpenCalls}" IsReadOnly="True" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="מזהה קריאה" Binding="{Binding Id}" />
                <DataGridTextColumn Header="סוג קריאה" Binding="{Binding CallType}" />
                <DataGridTextColumn Header="כתובת" Binding="{Binding FullAddress}" />
                <DataGridTextColumn Header="מרחק" Binding="{Binding CallDistance}" />
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="בחר" Command="{Binding DataContext.SelectCallCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Update Address + ReadOnly TextBox -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,10,0,0">
            <TextBlock Text="עדכון כתובת:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Text="{Binding VolunteerAddress}" Width="200" Margin="0,0,10,0"/>
            <Button Content="עדכן" Command="{Binding UpdateAddressCommand}" Margin="0,0,20,0"/>
            <TextBox Text="{Binding SomeProperty}" IsReadOnly="{Binding IsReadOnly}" Width="200"/>
        </StackPanel>
    </Grid>
</Window>