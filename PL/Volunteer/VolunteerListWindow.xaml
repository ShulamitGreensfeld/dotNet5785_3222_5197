<Window x:Class="PL.Volunteer.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Loaded="Window_Loaded" Closed="Window_Closed"
        Title="Volunteer List" Height="640" Width="900"
        Background="#F5F6FA">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="רשימת מתנדבים" FontSize="28" FontWeight="Bold"
                   Foreground="#3B5998" Margin="0,20,0,20" HorizontalAlignment="Center"/>

        <ListView Grid.Row="1"
                  ItemsSource="{Binding VolunteerList}"
                  SelectedItem="{Binding SelectedVolunteer, Mode=TwoWay}"
                  MouseDoubleClick="ListView_MouseDoubleClick"
                  Margin="20"
                  Background="White"
                  BorderBrush="#8B9DC3"
                  BorderThickness="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Id" Width="70" DisplayMemberBinding="{Binding Id}" />
                    <GridViewColumn Header="Full Name" Width="120" DisplayMemberBinding="{Binding FullName}" />
                    <GridViewColumn Header="Total Handled Calls" Width="110" DisplayMemberBinding="{Binding TotalHandledCalls}" />
                    <GridViewColumn Header="Total Canceled Calls" Width="110" DisplayMemberBinding="{Binding TotalCanceledCalls}" />
                    <GridViewColumn Header="Total Expired Calls" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TotalExpiredCalls}" Foreground="Red" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Call Id" Width="80" DisplayMemberBinding="{Binding CallId}" />
                    <GridViewColumn Header="Call Type" Width="110" DisplayMemberBinding="{Binding CallType}" />
                    <GridViewColumn Header="Is Active" Width="60">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsEnabled="False" IsChecked="{Binding IsActive}" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <!-- עמודת מחיקה -->
                    <GridViewColumn Header="מחיקה" Width="70">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="מחק"
                                        Click="DeleteButton_Click"
                                        Tag="{Binding Id}"
                                        Background="#E74C3C"
                                        Foreground="White"
                                        FontWeight="Bold"
                                        Padding="4,0"
                                        BorderThickness="0"
                                        Cursor="Hand"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <Button Content="הוספת מתנדב" Grid.Row="2"
                HorizontalAlignment="Center" Height="50" Width="150"
                Margin="0,20,0,20"
                Background="#3B5998" Foreground="White" FontWeight="Bold"
                Click="AddButton_Click"/>
    </Grid>
</Window>
