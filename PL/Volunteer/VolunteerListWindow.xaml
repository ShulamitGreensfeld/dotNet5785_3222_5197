<Window x:Class="PL.Volunteer.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Loaded="Window_Loaded" Closed="Window_Closed"
        Title="Volunteer List" Height="640" Width="1020" MaxWidth="1200"
        Background="#0D1117"
        ResizeMode="CanMinimize"
        FontFamily="Segoe UI">

    <Window.Resources>


        <!-- GitHub-style ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="ListViewItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1F6FEB"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#2A7FFF"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="#238636"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#161B22"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>

        <!-- GitHub-style Button -->
        <Style TargetType="Button" x:Key="ModernButton">
            <Setter Property="Background" Value="#238636"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#238636"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2EA043"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#3FB950"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#444C56"/>
                    <Setter Property="Foreground" Value="#CCCCCC"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>


        <!-- GitHub-style ListView -->
        <Style TargetType="ListView">
            <Setter Property="Background" Value="#161B22"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
        </Style>

        <!-- GitHub-style TextBlock -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Volunteer List" FontSize="28" FontWeight="Bold"
                   Foreground="#238636" Margin="0,20,0,20" HorizontalAlignment="Center"/>

        <!-- Filter -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <TextBlock Text="📋 Sort:" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,5,0"    Foreground="#238636" />
            <ComboBox Width="180" Height="30" 
                      ItemsSource="{Binding Source={StaticResource CallTypeCollectionKey}}"
                      SelectedItem="{Binding SelectedCallType, Mode=TwoWay}"/>
        </StackPanel>

        <!-- Volunteer List -->
        <ListView Grid.Row="1"
                  ItemsSource="{Binding VolunteerList}"
                  SelectedItem="{Binding SelectedVolunteer, Mode=TwoWay}"
                  MouseDoubleClick="ListView_MouseDoubleClick"
                  Margin="0,20,0,20"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="🆔 ID" Width="80" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="👤 Full Name" Width="140" DisplayMemberBinding="{Binding FullName}"/>
                    <GridViewColumn Header="📞 Handled Calls" Width="130" DisplayMemberBinding="{Binding TotalHandledCalls}"/>
                    <GridViewColumn Header="❌ Canceled Calls" Width="130" DisplayMemberBinding="{Binding TotalCanceledCalls}"/>
                    <GridViewColumn Header="⏰ Expired Calls" Width="130" DisplayMemberBinding="{Binding TotalExpiredCalls}"/>
                    <GridViewColumn Header="☎️ Treating Call ID" Width="120" DisplayMemberBinding="{Binding CallId}"/>
                    <GridViewColumn Header="📋 Call Type" Width="110" DisplayMemberBinding="{Binding CallType}"/>
                    <GridViewColumn Header="✔️ Active" Width="60">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsEnabled="False" IsChecked="{Binding IsActive}" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Add Button -->
        <Button Content="➕ Add Volunteer" Grid.Row="2"
                HorizontalAlignment="Center" Height="45" Width="180"
                Margin="0,20,0,20"
                Style="{StaticResource ModernButton}"
                Click="AddButton_Click"/>
    </Grid>
</Window>
