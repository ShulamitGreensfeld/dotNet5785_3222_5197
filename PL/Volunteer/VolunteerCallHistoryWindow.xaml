<Window x:Class="PL.VolunteerCallHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Volunteer Call History" Height="450" Width="800"
        Background="#0D1117"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- GitHub-style ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#161B22"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <!-- GitHub-style TextBlock -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <!-- GitHub-style DataGrid -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#161B22"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="RowBackground" Value="#161B22"/>
            <Setter Property="AlternatingRowBackground" Value="#1E2630"/>
            <Setter Property="GridLinesVisibility" Value="All"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#30363D"/>
            <Setter Property="VerticalGridLinesBrush" Value="#30363D"/>
            <Setter Property="Margin" Value="10"/>
        </Style>

        <!-- GitHub-style Column Headers -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#0D1117"/>
            <Setter Property="Foreground" Value="#2EA043"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Filter and Sort -->
        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="Filter by Call Type:" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding CallTypeCollection}" SelectedItem="{Binding SelectedCallType}" Width="150"/>
            <TextBlock Text="Sort by:" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding SortFields}" SelectedItem="{Binding SelectedSortField}" Width="150"/>
        </StackPanel>

        <!-- Calls -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding ClosedCalls}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Type" Binding="{Binding CallType}" Width="Auto"/>
                <DataGridTextColumn Header="Address" Binding="{Binding FullAddress}" Width="*"/>
                <DataGridTextColumn Header="Finish Time" Binding="{Binding End_time, StringFormat=dd/MM/yyyy HH:mm}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
